language: node_js
os: linux
dist: noble

node_js:
  - "24"

if: branch = main OR type = pull_request

cache: npm

env:
  global:
    - NEXT_TELEMETRY_DISABLED=1

install:
  - npm ci

script:
  - npm test
  - npm run lint
  - npm run build

notifications:
  email: false
